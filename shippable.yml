language: php

php:
 - "7.0"
 - "7.1"

build:
  pre_ci:
    - echo The PHP version is $SHIPPABLE_PHP_VERSION
  pre_ci_boot:
   image_name: drydock/u16phpall
   image_tag: master
   pull: false
  ci:
    - echo The PHP version is $SHIPPABLE_PHP_VERSION
